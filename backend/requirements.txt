# requirements.txt – PROVEN WORKING COMBINATION (November 2025)
fastapi==0.104.1
uvicorn[standard]==0.24.0

sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Critical: pin bcrypt to the last version that still has __about__
bcrypt==4.0.1
passlib[bcrypt]==1.7.4          # this line automatically pulls correct bcrypt too, but we force 4.0.1 anyway

python-jose[cryptography]==3.3.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic[email]
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Test dependencies (optional in production image)
pytest==7.4.3
httpx==0.25.2